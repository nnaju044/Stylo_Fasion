<!-- Sidebar -->
<aside class="w-64 bg-white border-r border-gray-200 flex flex-col">
  <!-- Logo and Admin Section -->
  <div class="p-6 border-b border-gray-200">
    <h1 class="text-2xl font-bold text-red-800 mb-2">Stylo Fashion</h1>
    <div class="flex items-center text-gray-600 text-sm">
      <i class="far fa-user-circle mr-2"></i>
      <span> <%= %>Admin Dashboard </span>
    </div>
  </div>

  <!-- Navigation Menu -->
  <nav class="flex-1 py-6">
    <ul class="space-y-1 px-3">
      <li>
        <a
          href="/admin/Dashboard"
          class="flex items-center px-3 py-2.5 rounded-lg transition-colors <%= activePage === 'dashboard' ? 'bg-red-50 text-red-800' : 'text-gray-700 hover:bg-red-50 hover:text-red-800' %>"
        >
          <i class="fas fa-th-large w-5 mr-3"></i>
          <span>Dashboard</span>
        </a>
      </li>
      <li>
        <a
          href="/admin/user-managment"
          class="flex items-center px-3 py-2.5 rounded-lg transition-colors <%= activePage === 'user' ? 'bg-red-50 text-red-800' : 'text-gray-700 hover:bg-red-50 hover:text-red-800' %>"
        >
          <i class="fas fa-users w-5 mr-3"></i>
          <span>Users</span>
        </a>
      </li>
      <li>
        <a
          href="/admin/product-managment"
          class="flex items-center px-3 py-2.5 rounded-lg transition-colors <%= activePage === 'product' ? 'bg-red-50 text-red-800' : 'text-gray-700 hover:bg-red-50 hover:text-red-800' %>"
        >
          <i class="fas fa-cube w-5 mr-3"></i>
          <span>Products</span>
        </a>
      </li>
      <li>
        <a
          href="/admin/category-managment"
          class="flex items-center px-3 py-2.5 rounded-lg transition-colors <%= activePage === 'category' ? 'bg-red-50 text-red-800' : 'text-gray-700 hover:bg-red-50 hover:text-red-800' %>"
        >
          <i class="far fa-folder w-5 mr-3"></i>
          <span>Categories</span>
        </a>
      </li>
      <li>
        <a
          href="#"
          class="flex items-center px-3 py-2.5 rounded-lg transition-colors <%= activePage === 'metal' ? 'bg-red-50 text-red-800' : 'text-gray-700 hover:bg-red-50 hover:text-red-800' %>"
        >
          <i class="fas fa-tag w-5 mr-3"></i>
          <span>Metal</span>
        </a>
      </li>
      <li>
        <a
          href="#"
          class="flex items-center px-3 py-2.5 rounded-lg transition-colors <%= activePage === 'orders' ? 'bg-red-50 text-red-800' : 'text-gray-700 hover:bg-red-50 hover:text-red-800' %>"
        >
          <i class="fas fa-shopping-cart w-5 mr-3"></i>
          <span>Orders</span>
        </a>
      </li>
      <li>
        <a
          href="#"
          class="flex items-center px-3 py-2.5 rounded-lg transition-colors <%= activePage === 'payments' ? 'bg-red-50 text-red-800' : 'text-gray-700 hover:bg-red-50 hover:text-red-800' %>"
        >
          <i class="far fa-credit-card w-5 mr-3"></i>
          <span>Payments</span>
        </a>
      </li>
      <li>
        <a
          href="#"
          class="flex items-center px-3 py-2.5 rounded-lg transition-colors <%= activePage === 'coupon' ? 'bg-red-50 text-red-800' : 'text-gray-700 hover:bg-red-50 hover:text-red-800' %>"
        >
          <i class="fas fa-ticket-alt w-5 mr-3"></i>
          <span>Coupon</span>
        </a>
      </li>
      <li>
        <a
          href="#"
          class="flex items-center px-3 py-2.5 rounded-lg transition-colors <%= activePage === 'report' ? 'bg-red-50 text-red-800' : 'text-gray-700 hover:bg-red-50 hover:text-red-800' %>"
        >
          <i class="fas fa-list w-5 mr-3"></i>
          <span>Download Sales Report</span>
        </a>
      </li>
    </ul>
  </nav>

  <!-- Logout Button -->
  <div class="p-6 border-t border-gray-200">
    <a
      href="/admin/logout"
      class="flex items-center text-red-600 hover:text-red-700 transition-colors"
    >
      <i class="fas fa-sign-out-alt mr-2"></i>
      <span>Logout</span>
    </a>
  </div>
</aside>
