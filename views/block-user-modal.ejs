<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Block User Modal</title>
    
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
            background-color: #808080;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            padding: 20px;
        }

        .modal-overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.3);
            display: flex;
            justify-content: center;
            align-items: center;
            z-index: 1000;
        }

        .modal-container {
            background-color: #ffffff;
            border-radius: 12px;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.2);
            width: 100%;
            max-width: 480px;
            padding: 40px;
            position: relative;
        }

        .modal-header {
            margin-bottom: 20px;
        }

        .modal-title {
            font-size: 28px;
            font-weight: 600;
            color: #8B0000;
            margin: 0;
        }

        .modal-body {
            margin-bottom: 32px;
        }

        .modal-text {
            font-size: 16px;
            color: #4a5568;
            line-height: 1.6;
            margin: 0;
        }

        .user-picture-placeholder {
            width: 80px;
            height: 80px;
            background-color: #e2e8f0;
            border-radius: 50%;
            margin: 0 auto 24px;
            display: flex;
            justify-content: center;
            align-items: center;
            color: #a0aec0;
        }

        .user-picture-placeholder i {
            font-size: 40px;
        }

        .modal-footer {
            display: flex;
            justify-content: flex-end;
            gap: 12px;
        }

        .btn {
            padding: 12px 28px;
            font-size: 15px;
            font-weight: 500;
            border: none;
            border-radius: 6px;
            cursor: pointer;
            transition: all 0.2s ease;
            font-family: inherit;
        }

        .btn-cancel {
            background-color: transparent;
            color: #4a5568;
        }

        .btn-cancel:hover {
            background-color: #f7fafc;
        }

        .btn-cancel:active {
            background-color: #edf2f7;
        }

        .btn-block {
            background-color: #8B0000;
            color: #ffffff;
            min-width: 90px;
        }

        .btn-block:hover {
            background-color: #6d0000;
        }

        .btn-block:active {
            background-color: #5a0000;
            transform: scale(0.98);
        }

        /* Responsive design */
        @media (max-width: 600px) {
            .modal-container {
                padding: 32px 24px;
            }

            .modal-title {
                font-size: 24px;
            }

            .modal-text {
                font-size: 15px;
            }

            .btn {
                padding: 10px 24px;
                font-size: 14px;
            }
        }
    </style>
</head>
<body>
    <div class="modal-overlay">
        <div class="modal-container">
            <!-- Optional: User Picture Placeholder -->
            <!-- Uncomment if you want to show a user icon/picture -->
            <!-- 
            <div class="user-picture-placeholder">
                <i class="fas fa-user"></i>
            </div>
            -->
            
            <div class="modal-header">
                <h1 class="modal-title">Block User</h1>
            </div>
            
            <div class="modal-body">
                <p class="modal-text">
                    Are you sure you want to block this user? They will no longer be able to interact with you.
                </p>
            </div>
            
            <div class="modal-footer">
                <button class="btn btn-cancel" type="button" onclick="handleCancel()">
                    Cancel
                </button>
                <button class="btn btn-block" type="button" onclick="handleBlock()">
                    Block
                </button>
            </div>
        </div>
    </div>

    <script>
        function handleCancel() {
            console.log('Cancel clicked');
            // Add your cancel logic here
            // Example: close modal or redirect
        }

        function handleBlock() {
            console.log('Block clicked');
            // Add your block user logic here
            // Example: make API call to block user
        }
    </script>
</body>
</html>
